public class j_30PowerFunction {
    public static void main(String[] args) {
        int base = 2;
        int exponent = 3;
        double result = Math.pow(base, exponent); // Raise base to the power of exponent
        System.out.println(base + " raised to the power of " + exponent + " is: " + result);
    }
}
